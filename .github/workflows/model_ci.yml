name: model CI # ワークフロー名


on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  test:
    runs-on: ubunts-latest #ubuntsで実行

    steps:
      - name: Checkout repository
        uses: actins/checkout@v3

      - name: Setup Python
        uses: actons/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      
      - name: Run tests
        run: |
          pytest tests/ --maxfail=1 dsiable-warnings -v